{
  "name": "ruyi-mcp",
  "version": "0.1.8",
  "description": "Community MCP server for ruyiPage browser automation, network inspection, fingerprint analysis, and WebDriver BiDi trace workflows",
  "private": true,
  "license": "MIT",
  "type": "module",
  "main": "build/src/index.js",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "check:python": "python -m py_compile bridge/ruyi_bridge.py scripts/runtime_ruyipage_1256.py scripts/runtime_capture_lifecycle.py scripts/runtime_attach_lifecycle.py",
    "check:contract": "python scripts/contract_bridge.py",
    "check:runtime": "python scripts/runtime_ruyipage_1256.py",
    "check:capture-runtime": "python scripts/runtime_capture_lifecycle.py",
    "check:attach-runtime": "python scripts/runtime_attach_lifecycle.py",
    "smoke": "node scripts/smoke-list-tools.mjs",
    "snapshot:tools": "npm run build && node scripts/smoke-list-tools.mjs --update-snapshot",
    "check:lifecycle": "node --test scripts/server-contract.test.mjs scripts/bridge-lifecycle.test.mjs scripts/stdio-lifecycle.test.mjs",
    "check": "npm run typecheck && npm run check:python && npm run check:contract && npm run build && npm run smoke && npm run check:lifecycle",
    "start": "node build/src/index.js",
    "dev": "tsc && node build/src/index.js"
  },
  "keywords": ["mcp", "mcp-server", "ruyipage", "browser-automation", "firefox", "webdriver-bidi", "network-inspection", "fingerprinting", "trace"],
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0"
  },
  "devDependencies": {
    "@types/node": "^24.3.3",
    "typescript": "^5.9.2"
  },
  "engines": {
    "node": ">=20"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Facetomyself/ruyi-mcp.git"
  },
  "bugs": {
    "url": "https://github.com/Facetomyself/ruyi-mcp/issues"
  },
  "homepage": "https://github.com/Facetomyself/ruyi-mcp#readme"
}
